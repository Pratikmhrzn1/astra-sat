/**
 * The design-system primitives, re-exported so a component imports one module
 * ('@/shared/ui') instead of one per element. Anything here is presentational
 * and knows nothing about features, routing or data fetching.
 */
export { Button } from './Button';
export { Card, CardHeader, CardBody, CardTitle } from './Card';
export { Badge, SubjectBadge, RoleBadge } from './Badge';
export { Input, Textarea, Select } from './Input';
export { Modal, ConfirmModal } from './Modal';
export { Spinner, PageLoader } from './Spinner';
